apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: carts-green-db-green
  labels:
    app: {{ .Chart.Name }}-selector-carts-db
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.cartsGreenDbGreen.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-carts-db
        deployment: carts-green-db-green
    spec:
      containers:
      - name: {{ .Values.cartsGreenDbGreen.container.name }}
        image: "{{ .Values.cartsGreenDbGreen.image.repository }}:{{ .Values.cartsGreenDbGreen.image.tag }}"
        imagePullPolicy: {{ .Values.cartsGreenDbGreen.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.cartsGreenDbGreen.image.repository }}:{{ .Values.cartsGreenDbGreen.image.tag }}"
        - name: KEPTN_PROJECT
          value: "{{ .Chart.Name }}"
        - name: KEPTN_STAGE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: KEPTN_SERVICE
          value: "{{ .Values.cartsGreenDbGreen.service.name }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.cartsGreenDbGreen.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}
